const { globalPrefix } = require('../../config.json');

module.exports = {
	command: `${globalPrefix}suggest`,
	description: 'Sends information related to where you can suggest ideas for Birdy Birdy.',
	usage1: `\`${globalPrefix}suggest\`
If you use this command, you can suggest stuff so my owner can make me better!`,
};